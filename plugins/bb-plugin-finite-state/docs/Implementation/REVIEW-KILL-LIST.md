# Review kill list

A rolling list of the blocker classes that actually appear in round-1 REQUEST_CHANGES verdicts on the task board. Maintained by the coordinator: one line per class, added when a new class produces a round-1 blocker, pruned when a class stops appearing. Implementers attest against this list when flagging review (see [AGENTS.md](./AGENTS.md), "The review kill list").

Last updated: 2026-08-14 (FS-126 round-1 verdict). Source sample: FS-49, FS-54, FS-74, FS-107, FS-108, FS-121, FS-122, FS-123, FS-124, FS-126, FS-127, FS-166, FS-168, FS-171, FS-172, FS-174, FS-179, FS-182, FS-183 — zero first-pass approvals; FS-108/122/124 reached round 3.

| #   | Class                                 | What it looks like                                                                                                                                                                                                                                                                                                                                | Round-1 examples                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Registered-surface bypass             | The property is enforced/tested on an exported helper while the registered RPC/tool/panel path bypasses it or never calls the new code at all — including tests that pass on reverted/defective source (a guard that does not bind)                                                                                                               | FS-122 (send-confirm gate bypassed by the actual RPC), FS-74 (guard defeated by runtime registration), FS-108 (parser never invoked by any production path), FS-168 repair (on-disk YAML assertions passed with the source reverted to the original defect), FS-126 (refusal proved by injecting a throwing fake — the real code never inspects `mount.readiness`; a test touched the exact wedged RPC but asserted only `runId`/`kind`)                                                                                                                                                                                                                                                                  |
| 2   | Real-data drift                       | Passes on mock/synthetic fixtures, wrong on real artifacts                                                                                                                                                                                                                                                                                        | FS-108 (wrong connectivity on the repo's own real KiCad fixture), FS-164 (real API nests the component object; mocks were flat), FS-174 (multi-segment groups and percent-encoded versions on real distro rows rejected or collided)                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 3   | Amendment/spec conformance drift      | Implementation contradicts signed amendment text                                                                                                                                                                                                                                                                                                  | FS-121 (AMD-0013 "one mechanism and one test" became two mechanisms), FS-74 (intake note said "nine" tools vs the ratified eight)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 4   | Concurrency/liveness edges            | Claim leaks, orphaned in-flight rows, missing liveness checks on persisted state, checkpoints outside the `try` so failed attempts leave no row — or orphaned state that permanently wedges the surface. Includes ambiguity/failure boundaries drawn one step past the true side-effect boundary, so at-least-once RPC failures still misclassify | FS-124 (claim leak, liveness), FS-154 (`publishChanged` outside the `try` orphans a `running` probe_run row), FS-171 (queued checkpoint outside the `try`; a throwing resolver records no run row), FS-172 (orphaned staging sqlite makes every later pull fail forever with no recovery surface), FS-182 (ambiguity gated on "job id parsed" instead of "dispatch issued"; a transport error after Forge accepted still said "retry" with nothing persisted), FS-126 (D1 read its observation off a dispatch receipt — every production verdict inconclusive-by-timing with a false "did not complete" persisted; and an oversized value in one column wedged the registered runs RPC page-wide forever) |
| 5   | Cross-scope leak / stale-scope UI     | Data from another project/scope leaks into the current view, or a scope switch leaves prior-scope evidence rendered                                                                                                                                                                                                                               | FS-171 (unfiltered `cachedProjectVersions` auto-selected another project's version; header/timeline/verdict card each rendered a different scope at once)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 6   | Silent failure on visible surfaces    | An action fails and the surface shows nothing — errors rendered only in states the user is not in, or not at all                                                                                                                                                                                                                                  | FS-168 (bulk-apply click produced no toast/file/error/log), FS-172 (`pullError` rendered only in the empty-state block; failed "Pull again" showed nothing), FS-182 (ambiguous run's alert gated on `status === "failed"` never renders; row stays `running` forever with no reconcile path)                                                                                                                                                                                                                                                                                                                                                                                                              |
| 7   | Guard fix trades one hole for another | A security-guard repair closes the reported vector but silently narrows coverage elsewhere in the same check — e.g. replacing `Object.hasOwn` with an enumerable-only scan. Attack the REPAIRED guard as adversarially as the original                                                                                                            | FS-183 (`__proto__`-key fix rebuilt the prohibition on `Object.entries`; a non-enumerable own key `fs_sync_push` — caught pre-fix — now passes), FS-122 repair (the "just a guard" repair shipped a ReDoS)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 8   | Flagged review with CI red            | Review flagged while the exact-head GitHub guard is failing — most often the changed-file Prettier ratchet. CI status is part of the attestation; verify with `check-changed-formatting.mjs` from the repo-root real path (a `/tmp` symlink invocation false-passes)                                                                              | FS-179 (4 of 6 changed files unformatted, guard red at flag time), FS-183 (2 files bounced by the ratchet before review), FS-185 (3 files, caught before flagging)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
